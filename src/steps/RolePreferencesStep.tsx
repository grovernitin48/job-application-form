// src/steps/RolePreferencesStep.tsx
import React from "react";

/**
 * Step 3: Role Preferences
 * Also schema-driven, with conditional Portfolio URLs logic.
 */
export const RolePreferencesStep: React.FC = () => {
  return <div>Role Preferences Step (placeholder)</div>;
};
